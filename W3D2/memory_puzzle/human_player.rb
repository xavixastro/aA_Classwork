
class HumanPlayer
    def get_pos_1(arr)
        puts "Please enter a position:"
        input = gets.chomp.split(" ").map(&:to_i)
    end

    def get_pos_2(arr)
        puts "Please enter a position:"
        input = gets.chomp.split(" ").map(&:to_i)
    end

    def receive_revealed_card(pos, val)
        
    end
end